[package]
name = "coreason-manifest-types"
version = "1.0.0"
edition = "2021"
description = "Rust bindings for the CoReason Hollow Data Plane."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
