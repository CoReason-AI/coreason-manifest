1. **Deprecate Legacy LWW Substrate (`ontology.py`)**:
   - Delete `PatchOperationProfile`.
   - Delete `StateMutationIntent`.
   - Delete `StateDifferentialManifest`.

2. **Introduce `TemporalConflictResolutionPolicy` & Update `GlobalGovernancePolicy`**:
   - Add `TemporalConflictResolutionPolicy` class.
   - Inject `temporal_conflict_policy: TemporalConflictResolutionPolicy | None` into `GlobalGovernancePolicy`.

3. **Introduce `TemporalEdgeInvalidationIntent` & Update `AnyIntent`**:
   - Add `TemporalEdgeInvalidationIntent` class.
   - Inject `TemporalEdgeInvalidationIntent` into `AnyIntent` discriminated union type alias.

4. **Introduce `TemporalGraphCRDTManifest` & Update `PersistenceCommitReceipt`**:
   - Add `TemporalGraphCRDTManifest` class, replacing `StateDifferentialManifest`. This class will include `_enforce_canonical_sort_crdt` method using `object.__setattr__` to deterministically sort array sets.
   - Update `PersistenceCommitReceipt`: change `committed_state_diff_cid` to `committed_temporal_crdt_cid` and its docstring reference.
   - Add model_rebuild calls at the bottom of the file.

5. **Implement the Merge Algebra (`algebra.py`)**:
   - Delete the `import jsonpatch` statement.
   - Delete `transmute_state_differential`.
   - Add the `transmute_temporal_crdt` function.

6. **Purge and Rewrite Test Suite - Part 1**:
   - Delete legacy JSON Patch tests in `tests/contracts/test_algebra_coverage.py` and `tests/fuzzing/test_algebra.py`.

7. **Purge and Rewrite Test Suite - Part 2**:
   - Add `test_crdt_idempotence` and `test_crdt_commutativity` tests to `tests/contracts/test_algebra_coverage.py` to prove the semilattice invariants. Make sure dummy IDs match the `did:coreason:.*` regex.

8. **Verification & Execution**:
   - Format and lint code: `uv run ruff format src/ tests/` and `uv run ruff check src/ tests/ --fix`.
   - Type check code: `uv run mypy src/ tests/`.
   - Test code: `uv run pytest`.
   - Prove Zero-Orphan invariant: `uv run python scripts/evaluate_topological_reachability.py`.

9. **Complete pre commit steps**:
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.

10. **Submit**:
    - Commit and push to branch with a descriptive message.
