name: OSV-Scanner
on:
  push:
    branches: [main, coreason-develop]
  pull_request:
    branches: [main, coreason-develop]
  schedule:
    - cron: '0 0 * * 1' # Weekly on Mondays

permissions:
  actions: read
  security-events: write
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
jobs:
  scan:
    name: OSV-Scanner
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@9a498708959aeaef5ef730655706c5a1df1edbc2 # v1.9.21


