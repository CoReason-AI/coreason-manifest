fix: Complete Counterfactual Engine implementation and cleanup

- Implemented tests for `InterventionalTaskIntent` and `CounterfactualReceipt` validating SSRF quarantine, array sorting, and mechanistic cascade constraints.
- Verified absence of hallucinated schemas and tests (`CryptographicAttestationReceipt`, `FederatedHandshakeIntent`, `ConnectionSeveranceEvent`, and `DecentralizedIdentityGateway`).
- Fixed missing docstrings, correct fields, and type rebuilding in `ontology.py`.
- Tested the code coverage ensuring a >99% pass rate in the current iteration.
